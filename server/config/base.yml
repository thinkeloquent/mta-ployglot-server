providers:
  llm:
    baseUrl: "https://api.example.com"
endpoints:
  llm001:
    name: "LLM Generate"
    baseUrl: "https://api.example.com/v1/generate"
    method: POST
    timeout: 30000
intent_mapping:
  default_intent: "llm001"
  mappings:
    "llm.generate": "llm001"
