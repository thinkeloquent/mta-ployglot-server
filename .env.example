# .env.example — copy to .env and adjust to taste.
#
# Port knobs consumed by docker-compose.yml. Container-internal ports are
# fixed (3000 for fastify, 8080 for fastapi); only the host-side mapping
# is configurable here.

FASTIFY_PORT=5100
FASTAPI_PORT=5200
