Invoke with:
use the app-yaml-config-pipeline agent to audit server/fastify/config/routes/
use the app-yaml-config-pipeline agent to explain how to consume yaml config from a new CLI script
use the app-yaml-config-pipeline agent to verify <handler.mjs> isn't re-walking the pipeline
