# SDK Scenarios

Embedded scenarios — drop these snippets into your code to use the package.

| #  | Scenario                                                     | Surface              |
| -- | ------------------------------------------------------------ | -------------------- |
| 01 | [Module-level usage](01-module-level.md)                     | `loadConfig` + `getFetchConfig` |
| 02 | [SDK class with `loadFromFile` + `refreshConfig` + `getByTag`](02-sdk-class.md) | `EndpointConfigSDK` |
