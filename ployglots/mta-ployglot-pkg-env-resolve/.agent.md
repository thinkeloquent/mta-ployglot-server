---
name: mta-ployglot-pkg-env-resolve
kind: polyglot-library-monorepo
schema: ployglot-agentmd-usages/v1
generated_by: ployglot-agentmd-usages
generated_at: 2026-04-27T00:00:00Z
layout: packages
---

# mta-ployglot-pkg-env-resolve — LLM Entry Reference

This repo ships **2 packages across 2 languages** — TypeScript and Python twin implementations of `env-resolve`, a four-tier configuration resolver (`arg → env → config → default`). Each package has its own `.agent.md`; this file is the index.

The behavior contract that both ports conform to lives at the repo root in [`SPEC.md`](./SPEC.md). Cross-language parity is enforced by [`tests/parity/fixtures.json`](./tests/parity/fixtures.json) — every drift fixture (`D1`, `D3`–`D7`) fails CI on either side if the implementations diverge.

## Languages

| Language | Package Count | Marker            |
|----------|---------------|-------------------|
| ts       | 1             | `tsconfig.json`   |
| py       | 1             | `pyproject.toml`  |

## Packages

| Key          | Language | Path           | Twin of           | `.agent.md`                                      |
|--------------|----------|----------------|-------------------|--------------------------------------------------|
| env-resolve  | ts       | `packages/ts`  | env-resolve (py)  | [link](./packages/ts/.agent.md)                  |
| env-resolve  | py       | `packages/py`  | env-resolve (ts)  | [link](./packages/py/.agent.md)                  |

Both packages are libraries (`publishable: true`). No CLI surface.

## Spec & Examples

- [`SPEC.md`](./SPEC.md) — canonical algorithm and the six aligned drift resolutions (D1, D3, D4, D5, D6, D7).
- [`examples/sdk/`](./examples/sdk/) — runnable scenarios with side-by-side TS + Python code.
- [`examples/api/`](./examples/api/) — per-function contract reference (signature, inputs, errors).
- [`tests/parity/fixtures.json`](./tests/parity/fixtures.json) — shared cross-language behavior fixtures (13 cases).

## Cross-Cutting Notes

See [`.agents/`](./.agents/) for:

- [parity.md](./.agents/parity.md) — twin drift catalog and aligned-spec rationale.
- [build-matrix.md](./.agents/build-matrix.md) — per-language build + test topology.

## Build & CI

The repo-root `make ci` runs both pipelines fail-fast:

```bash
make ci          # both languages (ci-ts → ci-py)
make ci-ts       # TypeScript only
make ci-py       # Python only
```

Each per-package Makefile exposes the eight standard targets — see [`.agents/build-matrix.md`](./.agents/build-matrix.md).

## Regenerating This File

Run the `/ployglot-agentmd-usages` skill from the repo root. It re-reads every package's `.agent.md` and rebuilds the tables here. Existing per-package `.agent.md` files are preserved unless stale.
