---
name: mta-ployglot-pkg-vault-file
kind: polyglot-monorepo
generated_by: ployglot-agentmd-usages
generated_at: 2026-04-24T00:00:00Z
---

# mta-ployglot-pkg-vault-file — LLM Entry Reference

This repo ships 2 packages across 2 languages. Each package has its own
`.agent.md`; this file is the index.

## Languages

| Language | Package Count | Marker            |
|----------|---------------|-------------------|
| ts       | 1             | `tsconfig.json`, `package.json` |
| py       | 1             | `pyproject.toml`                |

## Packages

| Key          | Language | Path            | Twin of         | `.agent.md`                                          |
|--------------|----------|-----------------|-----------------|------------------------------------------------------|
| vault-file   | ts       | `packages/ts`   | vault-file (py) | [packages/ts/.agent.md](./packages/ts/.agent.md)     |
| vault-file   | py       | `packages/py`   | vault-file (ts) | [packages/py/.agent.md](./packages/py/.agent.md)     |

Both packages expose the same vault-file + env-store SDK. Npm publishes
`@polyglot/vault-file`; PyPI publishes `polyglot-vault-file`. Behaviourally
interchangeable on wire format and error shape per the 12 aligned invariants
in `.agents/parity.md`.

## Cross-Cutting Notes

See [`.agents/`](./.agents/):

- [parity.md](./.agents/parity.md) — twin drift catalog, aligned invariants, residual idiomatic drift, enforcement mechanism.

(`build-matrix.md` omitted — no `.github/workflows/`. `conventions.md`
omitted — no CONVENTIONS.md or STYLE.md in the tree.)

## Regenerating This File

Run the `/ployglot-agentmd-usages` skill from the repo root. It re-reads
every package's `.agent.md` and rebuilds the tables here. Existing
per-package `.agent.md` files are preserved unless stale.
