# Mixed env file — set both SHARED and FILE_ONLY in the store.
# SHARED is also expected to be set in process.env during tests to prove
# vault-wins semantics (store value > env value).
SHARED=file-value
FILE_ONLY=only-in-file
QUOTED="wrapped in double quotes"
SINGLE_QUOTED='wrapped in single quotes'
